#include "main.h"
/**
 * _islower - Check the lowercase character
 * @c: Check the character
 * Return: 1 for for lowercase or 0 for anything elsee
 */

int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
return (0);
}

